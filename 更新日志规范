# 更新日志编写规范

## 核心原则

**以功能领域分类组织内容**。按 前端/后端/着色器/文档 等大类划分，每个类别下列出粗要点，要点下用子条目展开具体改动。

Tag 说明用一句话概括；日志文件在粗要点下展开具体做了什么、涉及哪些文件。

## 格式

```
# VX.Y.Z 更新日志

## 类别名（前端/后端/着色器/文档/…）
- 粗要点一句话概括
  - 具体改动 1
  - 具体改动 2

- 粗要点一句话概括
  - …

## 类别名
- …

## 修改文件清单
| 文件 | 操作 | 说明 |
|------|------|------|
| … | … | … |
```

## 示例（来自 V0.1.2）

```
## 前端
- 移除 localStorage 同步，后端不可达直接 toast 提示
  - 删除 `frontend/js/localSync.js`
  - `frontend/js/main.js` 改用 `loadFromServer`，try/catch 失败后弹出 toast

- 修复重命名竞态致 lastShader 丢失，以服务器返回值为准
  - `syncLastShaderToServer()` 改为返回 Promise，调用点 await 等待完成后再 refreshTree
  - 新增 `clearLastShaderOnServer()` 专门处理删除着色器场景
  - `/api/rename-shader` 响应返回 `lastShader`，前端直接使用
  - localStorage 为空时不做空值守卫，不向服务器发送 null

- 新建着色器 locked/cameraEnabled 默认 false
  - `locked` 默认 false：新建着色器不锁定，可直接编辑
  - `cameraEnabled` 默认 false：新建着色器不启用摄像机移动，需手动开启

- WebSocket 重命名后暂停 2 秒防中断
  - `modal.js` 新增 `pauseWsAutoReload()`，重命名完成后暂停自动重载，避免加载期间被打断

## 后端
- 新增 change.log 追加式操作日志
  - `backend/server.js`、`backend/prod-server.js` 新增 `appendLog()` 函数
  - 以 JSON 行格式追加，覆盖创建/删除/重命名/移动/锁定/摄像机/lastShader 等操作
  - 仅追加，不删除不更新
```

## 注意事项

- 同一类别的改动归在一起
- 粗要点一行概括，子条目写具体涉及的文件和函数
- Bug 修复在粗要点里直接说明修复了什么，子条目展开修复措施
- 末尾保留“修改文件清单”表格，便于快速查阅
- 类别顺序：前端 → 后端 → 着色器 → 文档 → 其他
