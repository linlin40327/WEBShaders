{
  "name": "shader3d",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,green \"cd backend && npm start\" \"cd frontend && npm run dev\"",
    "server": "cd backend && npm start",
    "build": "cd frontend && npm run build",
    "preview": "concurrently -n api,web -c cyan,green \"cd backend && npm start\" \"cd frontend && npm run preview\"",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
